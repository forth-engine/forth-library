#pragma once

namespace Forth
{
	namespace Physics
	{
		/*
    class Convex : Shape
    {

        Convex (ShapeDef def, Body body) : base(def, body, (ShapeType)3)
        {
            bounding = Bounds4(-def.Size, def.Size);
        }

        Vector4[] vertices;
        Bounds4 bounding;

        Bounds4 ComputeAABB(Transform4 tx)
        {
            return tx * bounding;
        }

        void ComputeMass(MassData& md)
        {
            throw NotImplementedException();
        }

        bool Raycast(Transform4 tx, ref RaycastHit4 raycast)
        {
            throw NotImplementedException();
        }

        bool TestPoint(Transform4 tx, Vector4 p)
        {
            throw NotImplementedException();
        }
    }
    */
	}
}